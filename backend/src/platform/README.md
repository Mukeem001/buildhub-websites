# Platform layer

This folder contains the shared platform logic for the SaaS backend.

Use this area for:
- website lifecycle operations
- publishing flow
- domain handling
- shared user/admin platform APIs

Do not put ecommerce/restaurant/hospital/portfolio business logic here.
Keep those in the module folders under `src/modules`.
